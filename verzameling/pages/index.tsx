import type { NextPage } from 'next'
import Head from 'next/head'
import addTest from './api/addTest'

import { PrismaClient } from '@prisma/client'

export async function getServerSideProps() {
  const prisma = new PrismaClient()
  return {
    props: {
      x: await prisma.test.findMany()
    }
  }
}

const Home: NextPage = ({ x }) => {
  return (
    <div>
      <Head>
        <title>Create Next App</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <form>
        <input type="text" name='name' placeholder='name' />
        <input type="number" name="age" placeholder='age' />
        <input type="number" name="score" step={0.1} placeholder='score' />
        <button type='submit'>Subbit</button>
      </form>
      <table>
        <tr>
          <th>id</th>
          <th>name</th>
          <th>age</th>
          <th>score</th>
        </tr>
        {x.map(i => (
          <tr key={i.id}>
            <td key={i.id + '-id'}>{i.id}</td>
            <td key={i.id + '-name'}>{i.name}</td>
            <td key={i.id + '-age'}>{i.age}</td>
            <td key={i.id + '-score'}>{i.score}</td>
          </tr>
        ))}
      </table>
    </div>
  )
}

export default Home
